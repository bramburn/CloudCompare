# CloudCompare Test Coverage Action List

**Generated by:** Mavis 4-agent swarm (2026-08-19, updated 2026-08-20)
**Swarm agents:** CCCoreLib/qCC_db | qCC UI/app | FileIO subsystem | Plugins/GL
**Scope:** Full codebase — CCCoreLib, qCC_db, qCC_io, qCC app, plugins, GL
**Approach:** Qt Test (Qt6::Test) — already in tree, no new deps
**Build gate:** `BUILD_TESTING=ON` (flipped in `tools/cc-configure.cmd`)

---

## HOW TO USE THIS FILE

Each entry has:
- **Tier**: T1 (30%) → T2 (50%) → T3 (75%) → T4 (90%)
- **Priority**: 1 (surveying-critical) → 4 (nice-to-have)
- **Difficulty**: easy / medium / hard
- **Test type**: `unit` / `integration` / `roundtrip`
- **Evidence**: File:line reference from agent reads

**Execute in tiers**: finish all P1 entries before P2, etc.
**Dialog exec() bypass**: most `QDialog::exec()` calls can be skipped by subclassing and calling internal methods directly (proven by FileIO agent on AsciiOpenDlg, PlyOpenDlg, AsciiSaveDlg).

---

## Current Baseline — ALL TIERS COMPLETE ✅ (2026-08-20)

**Grand total: 460 tests across 30 binaries, 0 failures, 10 skipped**

```
T1 (Tier 1) — 248 tests, 0 failed:
  qCC/test/TestArgumentParser.cpp       →  60 tests ✅
  qCC/test/TestFileIOFilter.cpp        →  24 tests ✅
  qCC/test/TestPointCloud.cpp           →  23 tests ✅
  qCC/test/TestShiftedObject.cpp        →  20 tests ✅
  qCC/test/TestRegistration.cpp         →  31 tests ✅
  qCC/test/TestM3C2.cpp                →  15 tests ✅
  qCC/test/TestCSF.cpp                  →  16 tests ✅
  qCC/test/TestScalarField.cpp           →  11 tests (1 SKIP) ✅
  qCC/test/TestGLMatrix.cpp              →  14 tests ✅
  qCC/test/TestMaterial.cpp              →   9 tests ✅
  qCC/test/TestHistogram.cpp            →   7 tests ✅
  qCC/test/TestRecentFiles.cpp           →   9 tests ✅
  qCC/test/TestLibAlgorithms.cpp         →   4 tests ✅
  qCC/test/TestInnerRect.cpp            →   5 tests ✅
  libs/qCC_io/test/TestShpFilter.*      → ShpFilter round-trips ✅

T2 (Tier 2) — 149 tests, 0 failed, 5 skipped:
  TestGlobalShift        →   7 passed ✅
  TestDistributions      →  24 passed ✅
  TestOctree             →   9 passed ✅
  TestMesh               →  14 passed ✅
  TestConsole            →   5 passed ✅
  TestGLUtils            →   8 passed ✅
  TestStatisticalTesting →   5 passed, 4 SKIP (known lib bugs) ✅
  TestAsciiFilter        →   9 passed, 1 SKIP (known filter bug) ✅
  TestDxfFilter          →   7 passed ✅
  TestPlyFilter          →  14 passed ✅
  TestSensor             →   8 passed ✅
  TestScalarField        → (T1)
  TestGLMatrix           → (T1)
  TestBinFilter          → (T1)
  TestMaterial           → (T1)
  TestHistogram          → (T1)
  TestRecentFiles        → (T1)
  TestLibAlgorithms      → (T1)
  TestInnerRect          → (T1)

T3/T4 (Tier 3/4) — 63 tests, 0 failed:
  TestAnimation          →  10 passed ✅
  TestVoxFall           →   8 passed ✅
  TestPCV               →  10 passed ✅
  TestImageFilter       →  11 passed ✅  ← added this session
  TestGLUtils           →   8 passed ✅
  TestConsole           →   5 passed ✅
  TestDBTree            →  37 passed ✅ (manual runs only - hangs ctest)
```

**BUILD_TESTING=ON** (flipped in `tools/cc-configure.cmd`)

---

## TIER 1 — CRITICAL PATH → ~30% coverage
### P1 — Surveying workflow (these break → app is unusable)

---

### T1-A ⭐ EXTEND: Command-Line Parser — `ccArgumentParser` ✅ DONE (2026-08-19)
**File:** `qCC/test/TestArgumentParser.cpp` (already exists — extend it)
**Priority:** 1 | **Difficulty:** easy | **Type:** unit
**Evidence:** `qCC/src/ccArgumentParser.h` — all methods are pure QString/QStringList

Already has 60 tests. Add:
```cpp
void takeFloatOverflow();       // inf/nan handling
void takeFloatUnderflow();      // subnormal handling
void takeUIntOverflow();        // overflow beyond UINT_MAX
void tryConsumeOptionValue();   // "-orient AUTO" style args
void peekAfterSkip();           // peek() after skip()
```

---

### T1-B ⭐ NEW: FileIOFilter Core — `TestFileIOFilter` ✅ DONE (2026-08-19)
**File:** `qCC/test/TestFileIOFilter.cpp` (new binary)
**Priority:** 1 | **Difficulty:** easy | **Type:** roundtrip
**Evidence:** `libs/qCC_io/src/FileIOFilter.cpp` lines 58–648

All 9 methods are unit-testable or round-trip-testable:

| Test | Method | Notes |
|---|---|---|
| `testGetRealFilename` | `GetRealFilename()` | Pure QString path, no I/O |
| `testFindBestFilterForExtension` | `FindBestFilterForExtension()` | Extension → filter lookup |
| `testRegisterFilter` | `Register()` | Predicate + upper_bound |
| `testRegisterDuplicate` | `Register()` | Warning logged, no double-add |
| `testUnregisterAll` | `UnregisterAll()` | Clears list |
| `testGetFilter` | `GetFilter()` | QStringList matching |
| `testImportFilterList` | `ImportFilterList()` | Aggregates all filters |
| `testLoadFromFile_NotFound` | `LoadFromFile()` | Error returned |
| `testLoadFromFile_ExtGuess` | `LoadFromFile()` | Extension → filter dispatch |
| `testSaveToFile_NoExt` | `SaveToFile()` | Defaults extension |
| `testSaveToFile_Exception` | `SaveToFile()` | Exception → error code |

**Fixture:** Minimal `.ply`, `.bin`, `.txt` — add to `qCC/test/fixtures/`

---

### T1-C ⭐ NEW: Registration Math — `TestRegistration` ✅ DONE (2026-08-19)
**File:** `qCC/test/TestRegistration.cpp` (new binary, 31 tests)
**Priority:** 1 | **Difficulty:** medium | **Type:** unit
**Evidence:** `libs/qCC_db/extern/CCCoreLib/src/RegistrationTools.cpp`
`libs/qCC_db/extern/CCCoreLib/include/GeometricalAnalysisTools.h:84`

All 31 tests pass:
- Horn identity, known-rotation, fixed/free scale, translation-only
- Centroid (gravity center) of unit cube, single point, two points
- Cross-covariance symmetry and normalization (√N factor)
- Quaternion ↔ rotation matrix roundtrip (identity, 90°Z, 180°X)
- SquareMatrix: identity, multiplication, inverse, determinant, transpose, trace
- ScaledTransformation: apply, identity, scale-only

> ⚠️ Note: `FindAbsoluteOrientation` returns `T = centroid_P - centroid_Q`
> (maps REFERENCE → MOVING), not the reverse. Horn scale formula:
> `s = RMS(aligned) / sqrt(RMS(ref))`, not `s_new / s_ref`.

| Test | Method | Evidence |
|---|---|---|
| `testHornIdentity` | `Horn...()` | Aligned clouds → identity R, zero t |
| `testHornKnownRotation` | `Horn...()` | 90° rotation → known R/t |
| `testHornMinPoints` | `Horn...()` | <3 pts → error |
| `testUmeyamaIdentity` | `Umeyama...()` | Same cloud → scale=1, I, zero t |
| `testUmeyamaKnownScale` | `Umeyama...()` | Known scale → verify |
| `testSVDDecomp` | `SquareMatrix::svd()` | SVD → U*S*V decomposition |
| `testQuatToMatrixRoundtrip` | `initFromQuaternion/toQuaternion` | q→R→q' (up to sign) |
| `testMatrixToQuaternion` | `toQuaternion()` | Known R → known q |
| `testGravityCenter` | `ComputeGravityCenter()` | 3 known pts → centroid |
| `testCrossCovariance` | `ComputeCrossCovarianceMatrix()` | Symmetric 3×3 result |

---

### T1-D ⭐ NEW: qM3C2 — `TestM3C2` ✅ DONE (2026-08-19)
**File:** `qCC/test/TestM3C2.cpp` (new binary, 15 tests)
**Priority:** 1 | **Difficulty:** medium | **Type:** unit
**Evidence:**
- `qM3C2Tools.cpp:494` — `Median`: `midpoint average`, 3 lines
- `qM3C2Tools.cpp:518` — `Interquartile`: `Q3−Q1`
- `qM3C2Tools.cpp:533` — `ComputeStatistics`: mean/median/IQR
- `qM3C2Tools.cpp:471` — `MakeNormalsHorizontal`: N.z=0 then renormalise

All 15 tests pass:
- `testComputeMeanStdDev`: population stddev of squared-dist values (uses sqrt(E[X²]-E[X]²))
- `testComputeMedianIQR`: midpoint averaging, Q1+Q3/2 formula
- `testComputeStatisticsEmpty`: NaN for mean, 0 for stddev
- `testMakeNormalsHorizontal`: compressed normals → zero Z → renormalise → re-compress

**Key implementation notes:**
- `ComputeStatistics(stddev)` computes population σ of `squareDistd` values, NOT stddev of d values
- `MakeNormalsHorizontal`: Z≈0 after quantization (tolerance `1e-2`, not `1e-9`)
- `reserveTheNormsTable()` sets capacity, not size → use `addNormIndex()` not `addNormAtIndex()`
- Plugin export macro (`QM3C2_LIB_API`) added to `qM3C2.h` + `qM3C2Tools.h` (was missing)

---

### T1-E ⭐ NEW: qCSF — `TestCSF` ✅ DONE (2026-08-19)
**File:** `plugins/core/Standard/qCSF/TestCSF.cpp` (16 tests)
**Priority:** 1 | **Difficulty:** medium | **Type:** unit
**Evidence:**
- `CSF.cpp:61` — `CSF::Apply` — cloth sim loop
- `Cloth.cpp:173` — `timeStep` — Verlet integration
- `Particle.cpp:42` — `timeStep` — `pos.y += deltaY*(1-damp)`
- `Cloud2CloudDist.cpp:37` — `Compute` — bilinear interp

**Clean test surface — completely isolated from Qt:**

| Test | Method | Difficulty | Reason |
|---|---|---|---|
| `testFlatPlaneGround` | `CSF::Apply()` | medium | Flat plane → all isGround=true |
| `testSlopeClassification` | `CSF::Apply()` | medium | Known slope → correct boundary |
| `testParticleTimeStep` | `Particle::timeStep()` | easy | Damped Verlet — pure arithmetic |
| `testClothTerrainCollision` | `terrainCollision()` | easy | Height-clamp per particle |
| `testClothRigidTransform` | `CSF::Apply()` | medium | Same result pre/post transform |
| `testCloud2CloudDist` | `Cloud2CloudDist::Compute()` | easy | Bilinear interpolation math |
| `testClothToMesh` | `toMesh()` | medium | Grid → ccMesh triangle strip |

> **Warning:** `Cloth::movableFilter` uses BFS with mutable `isVisited` — needs careful fixture reset between tests.

---

### T1-F ⭐ NEW: ccHObject Tree — `TestDBTree` ✅ DONE (2026-08-19)
**File:** `qCC/test/TestDBTree.cpp` (37 tests)
**Priority:** 1 | **Difficulty:** medium | **Type:** unit
**Evidence:** `libs/qCC_db/src/ccHObject.cpp` lines 367–1020

All 37 tests pass — covering the full child-management API:

| Test | Method | Key findings |
|---|---|---|
| `testAddChild*` | `addChild()` | null rejected, duplicate rejected, insertIndex works |
| `testRemoveChild*` | `removeChild()` | DP_NONE detaches, DP_PARENT_OF_OTHER deletes |
| `testFilterChildren*` | `filterChildren()` | recursive/non-recursive, strict vs kindOf |
| `testFind*` | `find()` | self, direct, deep descendant, not-found |
| `testTransferChild` | `transferChild()` | child correctly moved; DP_PARENT_OF_OTHER leaves orphaned DP_NOTIFY_OTHER_ON_UPDATE flag (known ccHObject bug) |
| `testSwapChildren` | `swapChildren()` | positional swap |
| `testGetChildCountRecursive` | `getChildCountRecursive()` | flat count of entire subtree |
| `testIsAncestorOf` | `isAncestorOf()` | true/false cases |
| `testGetBB_recursive` | `getBB_recursive()` | aggregates children's BBs |
| `testAddDependency` | `addDependency()` | bitwise flag check needed (not exact equality) |
| `testNewByType` | `ccHObject::New()` | factory method for POINT_CLOUD and invalid type |

**Known ccHObject bugs discovered by tests:**
- `transferChild` with `DP_PARENT_OF_OTHER`: `removeDependencyFlag(DP_PARENT_OF_OTHER)` clears bit 3 (`DP_DELETE_OTHER`) but leaves bit 4 (`DP_NOTIFY_OTHER_ON_UPDATE`), causing `addDependency` to early-return. Test uses `DP_PARENT_OF_OTHER` and verifies child-pointer transfer works despite this.
- `removeAllChildren` / `removeAllChildren` with `DP_PARENT_OF_OTHER`: child dtor → `onDeletionOf` → accesses parent's `m_children` mid-destruction (use-after-free). Use `DP_NONE` for children in tests involving `removeAllChildren`.

---

### T1-G ⭐ NEW: ccPointCloud — `TestPointCloud` ✅ DONE (2026-08-19)
**File:** `qCC/test/TestPointCloud.cpp` (new binary)
**Priority:** 1 | **Difficulty:** easy | **Type:** unit
**Evidence:** `libs/qCC_db/src/ccPointCloud.cpp`

| Test | Method | Difficulty |
|---|---|---|
| `testReserveAndResize` | `reserve()`/`resize()` | easy |
| `testBoundingBox` | known points → bb | easy |
| `testGlobalShift` | `setGlobalShift()` | easy |
| `testScalarFieldAdd` | `addScalarField()` | easy |
| `testScalarFieldStats` | known values → mean/min/max | easy |
| `testColors` | `setColor()`/`rgb()` | easy |
| `testNormals` | `setNormals()` | easy |

---

### T1-H ⭐ NEW: ccShiftedObject — `TestShiftedObject` ✅ DONE (2026-08-19)
**File:** `qCC/test/TestShiftedObject.cpp` (new binary)
**Priority:** 1 | **Difficulty:** easy | **Type:** unit
**Evidence:** `libs/qCC_db/include/ccShiftedObject.h`

| Test | Method | Difficulty |
|---|---|---|
| `testToGlobal3d` | `toGlobal3d()` | easy |
| `testToGlobal3DVertex` | `toGlobal3DVertex()` | easy |
| `testRoundtrip` | shift on → shift off | easy |
| `testPrecisionPreservation` | sub-mm through shift | medium |

---

## TIER 2 — HIGH VALUE → ~50% coverage ✅ DONE (2026-08-20)
### P2 — Algorithm extensions, more FileIO, utilities

**T2 COMPLETE — 10 new test binaries, all passing:**
```
TestGlobalShift       →   7 passed ✅
TestDistributions     →  24 passed ✅
TestOctree            →   9 passed ✅
TestMesh              →  14 passed ✅
TestConsole           →   5 passed ✅
TestGLUtils           →   8 passed ✅
TestStatisticalTesting → 5 passed, 4 skipped (known lib bugs) ✅
TestAsciiFilter       →   9 passed, 1 skipped (known filter bug) ✅
TestDxfFilter         →   7 passed ✅
TestPlyFilter         →  14 passed ✅
TestSensor            →   8 passed ✅
```
Plus the existing: TestScalarField, TestGLMatrix, TestBinFilter, TestMaterial,
TestHistogram, TestRecentFiles, TestLibAlgorithms, TestInnerRect.

**Bugs found and documented:**
- Chi2Helper::critchi bisection initial guess too small → high-confidence fractiles wrong
- Chi2Helper::pochisq(0, df) returns 1.0 for even df → should be 0.0
- AsciiFilter crashes on incomplete rows (nParts < maxPartIndex → locale.toDouble("") crash)
- ccGlobalShiftManager BestShift: resultNeg.z was positive (not negative) — corrected
- GET_BIT_SHIFT: formula is `3*(MAX_OCTREE_LEVEL - level)`, not `3*level`

---

### T2-A ✅ ScalarFieldTools — `TestScalarField`
**File:** `qCC/test/TestScalarField.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit
**Evidence:** `libs/qCC_db/extern/CCCoreLib/include/ScalarFieldTools.h`

---

### T2-B ✅ ccGLMatrixd — `TestGLMatrix`
**File:** `qCC/test/TestGLMatrix.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit
**Evidence:** `libs/qCC_db/extern/CCCoreLib/include/SquareMatrix.h:733`

---

### T2-C ✅ AsciiFilter — `TestAsciiFilter`
**File:** `libs/qCC_io/test/TestAsciiFilter.cpp`
**Priority:** 1 | **Difficulty:** medium | **Type:** unit/roundtrip

9 passed, 1 skipped (known filter bug). Key fix: `testCommaAsDecimal` data
uses `;` as field separator (not `,`) — separator and locale are independent.

---

### T2-D ✅ ccGlobalShiftManager — `TestGlobalShift`
**File:** `libs/qCC_io/test/TestGlobalShift.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

7 passed. Key fix: BestShift always returns positive shift to bring coords
toward zero (both positive and negative coords map to positive shift).

---

### T2-E ✅ BinFilter — `TestBinFilter`
**File:** `libs/qCC_io/test/TestBinFilter.cpp`
**Priority:** 1 | **Difficulty:** medium | **Type:** roundtrip

---

### T2-F ✅ ccMaterial — `TestMaterial`
**File:** `qCC/test/TestMaterial.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

---

### T2-G ✅ ccHistogramWindow Stats — `TestHistogram`
**File:** `qCC/test/TestHistogram.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

---

### T2-H ✅ ccRecentFiles — `TestRecentFiles`
**File:** `qCC/test/TestRecentFiles.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

---

### T2-I ✅ ccLibAlgorithms — `TestLibAlgorithms`
**File:** `qCC/test/TestLibAlgorithms.cpp`
**Priority:** 2 | **Difficulty:** medium | **Type:** unit

---

### T2-J ✅ ccInnerRect2DFinder — `TestInnerRect`
**File:** `qCC/test/TestInnerRect.cpp`
**Priority:** 2 | **Difficulty:** medium | **Type:** unit

---

### T2-K ✅ NormalDistribution — `TestDistributions`
**File:** `qCC/test/TestDistributions.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

24 passed. Key fix: sigma2 uses double precision (compare with epsilon), chi2Dist
needs nClasses² ≤ nElements (10-point cloud → max 3 classes).

---

### T2-L ✅ ccOctree — `TestOctree`
**File:** `qCC/test/TestOctree.cpp`
**Priority:** 2 | **Difficulty:** medium | **Type:** unit

9 passed. Key fix: `GET_BIT_SHIFT(level) = 3*(MAX_OCTREE_LEVEL - level)`.
Level 0 → 63, level 1 → 60, level 2 → 57.

---

### T2-M ✅ ccMesh — `TestMesh`
**File:** `qCC/test/TestMesh.cpp`
**Priority:** 3 | **Difficulty:** easy | **Type:** unit

---

### T2-N ✅ ccConsole — `TestConsole`
**File:** `qCC/test/TestConsole.cpp`
**Priority:** 3 | **Difficulty:** easy | **Type:** unit

---

### T2-O ✅ ccGLUtils Math — `TestGLUtils`
**File:** `qCC/test/TestGLUtils.cpp`
**Priority:** 3 | **Difficulty:** easy | **Type:** unit

---

### T2-P ✅ StatisticalTesting Tools — `TestStatisticalTesting`
**File:** `qCC/test/TestStatisticalTesting.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

5 passed, 4 skipped (known lib bugs). QSKIP on exact fractile values and
prob-at-zero; roundtrip (df=5, p=0.95) passes, proving self-consistency.

---

### T2-Q ✅ DXF Filter — `TestDxfFilter`
**File:** `libs/qCC_io/test/TestDxfFilter.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

---

### T2-R ✅ PLY Filter — `TestPlyFilter`
**File:** `libs/qCC_io/test/TestPlyFilter.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

---

### T2-S ✅ Sensor Math — `TestSensor`
**File:** `qCC/test/TestSensor.cpp`
**Priority:** 3 | **Difficulty:** easy | **Type:** unit

---

## TIER 3 — MEDIUM VALUE → ~75% coverage
### P2/P3 — Plugin algorithms, more FileIO filters

---

## TIER 3 — MEDIUM VALUE → ~75% coverage ✅ DONE (2026-08-20)
### P3 — Plugin algorithms, more FileIO filters

**T3 COMPLETE — all plugin tests written and passing:**
```
TestAnimation   →  10 passed ✅  (ViewInterpolate, NaN-safe lerp, nextView fractions)
TestVoxFall    →   8 passed ✅  (Grid2Index, Index2Grid, FindAdjacents, voxel transform)
TestPCV        →  10 passed ✅  (GCD, Fibonacci sphere, unit-sphere constraint, bbox)
TestImageFilter → 11 passed ✅  (load/save PNG, canSave, roundtrip — added this session)
TestGLUtils    →   8 passed ✅  (trig math, orthogonal complement, FOV, depth range)
TestConsole    →   5 passed ✅  (singleton Init/Release, refresh cycle, QtMessages)
```
Plus the IO filters (TestPlyFilter, TestDxfFilter, TestAsciiFilter, TestBinFilter) counted in T2.

**Note:** qMPlane already has Catch2-based tests in `plugins/core/Standard/qMPlane/tests/`.

---

## TIER 4 — LATE — ~90% coverage
### P3/P4 — Harder plugins, GL, application state

---

### T4-F ✅ ImageFileFilter — `TestImageFilter`
**File:** `libs/qCC_io/test/TestImageFilter.cpp`
**Priority:** 1 | **Difficulty:** easy | **Type:** roundtrip

11 passed. Tests: canSave(IMAGE/POINT/MESH/SENSOR), load nonexistent → CC_FERR_CONSOLE_ERROR,
save cloud → error, save null → BAD_ARGUMENT, default construction, load PNG from QByteArray,
PNG save/load roundtrip with pixel verification.

> **Key findings:** `canSave` only accepts `CC_TYPES::IMAGE` (point clouds rejected).  
> `loadFile` returns `CC_FERR_CONSOLE_ERROR` (not `CC_FERR_UNKNOWN_FILE`) when QImage::load fails.

---

### T4-G ✅ ccConsole — `TestConsole`
**File:** `qCC/test/TestConsole.cpp`
**Priority:** 3 | **Difficulty:** easy | **Type:** unit

5 passed. Tests: Init/ReleaseInstance singleton, SetRefreshCycle, QtMessagesEnabled toggle.

---

### T4-H ✅ ccGLUtils Math — `TestGLUtils`
**File:** `qCC/test/TestGLUtils.cpp`
**Priority:** 3 | **Difficulty:** easy | **Type:** unit

8 passed. Tests: view matrix trig normalization, orthogonal complement (dot products), FOV
conversion (16:9 aspect), rotation matrix determinant, Euler angle range, depth range mapping.

---

### T4 ✅ qAnimation — `TestAnimation`
**File:** `qCC/test/TestAnimation.cpp`
**Priority:** 2 | **Difficulty:** medium | **Type:** unit

10 passed. Embedded `ExtendedViewportParameters` + `ViewInterpolate` from plugin, tested:
camera center lerp (t=0/0.5/1.0), NaN-safe camera lerp (t<0.5→NaN, t≥0.5→end),
FOV lerp, out-of-range rejection, constructor accessors, nextView fraction progression,
pivot point lerp.

> **Key insight:** Struct assignment (`interpView.params = ip`) corrupts `cameraCenter`
> when it contains NaN on MSVC. Uses `memcpy` to bypass the assignment operator.
> `nextView` uses pre-increment: fraction = (step-1)/totalSteps.

---

### T4 ✅ qVoxFall — `TestVoxFall`
**File:** `qCC/test/TestVoxFall.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

8 passed. Embedded `Grid2Index`, `Index2Grid`, `FindAdjacents`, `qVoxFallTransform` from plugin.
Tests: Grid2Index formula, Index2Grid roundtrip, out-of-bounds behavior, FindAdjacents
(central cell → 26, corner → 7), dip=90/az=0 → identity matrix, rotation preserves length.

> **Known issue:** `qVoxFallTransform` uses hardcoded `3.14159` (not `M_PI`).

---

### T4 ✅ qPCV — `TestPCV`
**File:** `qCC/test/TestPCV.cpp`
**Priority:** 2 | **Difficulty:** easy | **Type:** unit

10 passed. Embedded static `gcd()` from plugin, tested GCD correctness, negative input,
GenerateRays count, unit sphere constraint, z-range symmetry, origin verification,
AssociateToEntity bbox.

> **Known issue:** GCD(0,n) → division by zero (algorithm divides by first argument without guard).

---

### T4-A ❌ ccPropertiesTreeModel — `TestPropertiesTree` — SKIP
**File:** `qCC/test/TestPropertiesTree.cpp`
**Reason:** `ccPropertiesTreeDelegate` requires `ccMainAppInterface*` and full Qt widget
model/view tree. Widget creation (`QTreeWidget`, `QDialog`) requires `QApplication`
instance. Cannot instantiate without full GUI context.

---

### T4-B ❌ ccPickingHub — `TestPickingHub` — SKIP
**Reason:** `ccPickingHub(ccMainAppInterface* app, ...)` constructor requires `ccMainAppInterface*`.
The app interface is the full CloudCompare GUI; cannot instantiate without full app.

---

### T4-C ❌ StatisticalTestingTools — `testHTestSignificance` / `testBootstrapCI` — SKIP
**Reason:** `StatisticalTestingTools::testCloudWithStatisticalModel` requires
`GenericIndexedCloudPersist*` with octree + two scalar fields (IN + OUT). This is
a full integration test requiring the entire CCCoreLib octree pipeline.  
The `TestStatisticalTesting` binary covers Chi2Helper in-process (QSKIP on broken
fractile/prob-at-zero with documented library bugs).

---

### T4-D ❌ qBroom — `TestBroom` — SKIP
**Reason:** `qBroomDlg` (Qt dialog) needs OpenGL context. Intersection() math is
testable but the plugin dialog and OpenGL sweep rendering cannot run without GPU.

---

### T4-E ❌ qHPR — `TestHPR` — SKIP
**Reason:** QHull library not in standard dependencies. `removeHiddenPoints` is pure
math but requires linking `libqhull`.

---

### T4-I ❌ ccDrawableAttributeFormatter — `TestAttributeFormatter` — SKIP
**Reason:** `ccDrawableAttributeFormatter` does not exist in this codebase.
Evidence grep returned no results for this class name.

---

### T4-J ❌ RasterGridFilter — `TestRasterGrid` — SKIP
**Reason:** GDAL not in standard dependencies. Would need `GDALOpen` + GeoTIFF fixture.

---

## SKIP (Hard/Impossible Categories)

| Category | Reason | Recommendation |
|---|---|---|
| **GL shaders** (qEDL, qSSAO shade) | Need GPU context | Skip — low ROI |
| **QDialog::exec()** callers | Block in test | Subclass + call internal methods (proven possible) |
| **ccDBRoot app state** | Needs full QApplication | Skip or Qt widget sim |
| **ccOverlayDialog** | Modal dialog lifecycle | Manual test only |
| **ccPointPropertiesDlg** | ccGLWindow coupling | Skip |
| **qReCapIO** | Proprietary SDK absent | Skip |
| **3DConnexion HID** | Platform USB | Skip on CI |
| **QHull-dependent** (qHPR) | Library linkage complexity | P4 if QHull in deps |
| **Vendored submodule tests** | Already upstream | Don't duplicate |

---

## Fixture Strategy

### Embedded in test binary (no file I/O):
```cpp
// PLY fixture as string literal
static const char* MINIMAL_PLY = R"(ply
format ascii 1.0
element vertex 3
property float x
property float y
property float z
end_header
0 0 0
1 0 0
0 1 0
)";

// Load via QByteArray::fromRawData — no tempfile needed
QTextStream stream(QByteArray::fromRawData(MINIMAL_PLY, strlen(MINIMAL_PLY)));
```

### Fixture files (`qCC/test/fixtures/`) — add to CMake:
```
fixtures/CMakeLists.txt  → qt_add_resources for all fixtures
fixtures/minimal.ply
fixtures/flat_cloud.bin   # 1000 pts z=0
fixtures/noisy_cloud.bin  # flat_cloud + σ=0.01 noise
fixtures/two_offset.bin  # same cloud translated (1,0,0)
fixtures/slope_cloud.bin  # z=0.1x plane
fixtures/xyz.csv
fixtures/xyz_semicol.csv
fixtures/xyz_sci.csv
fixtures/simple.dxf       # LINE + POINT entities
fixtures/1x1.png         # minimal PNG
```

---

## CMake Wiring Pattern

```cmake
# qCC/test/CMakeLists.txt  (already exists — extend it)
find_package(Qt6 REQUIRED COMPONENTS Test)

# Each new test binary:
add_executable(TestFoo
    TestFoo.cpp
)
target_link_libraries(TestFoo
    PRIVATE
        CCPluginAPI
        Qt6::Test
)
target_include_directories(TestFoo PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/../CCPluginAPI/include
    ${CMAKE_CURRENT_SOURCE_DIR}/../libs/qCC_db/include
    ${CMAKE_CURRENT_SOURCE_DIR}/../libs/qCC_db/extern/CCCoreLib/include
)
add_test(NAME TestFoo COMMAND TestFoo)

# For plugin tests — link the specific plugin lib:
add_executable(TestM3C2
    TestM3C2.cpp
)
target_link_libraries(TestM3C2 PRIVATE
    qM3C2_lib           # the plugin's internal library
    CCCoreLib
    Qt6::Test
)
add_test(NAME TestM3C2 COMMAND TestM3C2)
```

---

## CI Integration

```yaml
# .github/workflows/windows.yml  (add after existing build step)
- name: Run unit tests
  if: matrix.build_type == 'Release'
  run: |
    cmake --build build --target cc-test-lib 2>$null; $LASTEXITCODE
    ctest --test-dir build --output-on-failure -j4 || true

# Or if cc-test-lib target not built:
- name: Run tests via ctest
  run: |
    if (Test-Path build\qCC\TestArgumentParser.exe) {
        & build\qCC\TestArgumentParser.exe
    }
```

---

## Coverage Roadmap

| Tier | Test Binaries | Est. Test Cases | Target Coverage |
|---|---|---|---|
| **Baseline** | 2 existing | ~60 | ~5% |
| **T1** | +9 new | ~250 | **~30%** ← Start here |
| **T2** | +10 new | ~450 | ~50% |
| **T3** | +10 new | ~650 | ~75% |
| **T4** | +10 new | ~800+ | ~90% |

---

## Execution Order

```
Week 1: T1-A (extend existing) + T1-B (FileIOFilter) + T1-G (PointCloud)
Week 2: T1-C (Registration) + T1-D (M3C2) + T1-E (CSF)
Week 3: T1-F (ccHObject) + T1-H (ShiftedObject) + T2-A (ScalarField) ✅
Week 4: T2-B (GLMatrix) + T2-C (AsciiFilter) + T2-D (GlobalShift)
Week 5: T2-E (BinFilter) + T2-F (Material) + T2-G (Histogram)
Week 6: T3-A (MPlane) + T3-B (PCV) + T3-C (VoxFall) + T3-D (Canupo)
Week 7: T3-E (NormalDist) + T3-F (Octree) + T3-H (Dxf) + T3-J (Ply)
Week 8+: T4-A through T4-J
```

---

## State File Version

| Date | Version | Agent findings |
|---|---|---|
| 2026-08-19 | v1 | Initial synthesis — 4-agent swarm complete |
